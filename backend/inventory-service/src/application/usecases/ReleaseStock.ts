// import { IInventoryRepository } from "../../domain/repositories/IInventoryRepository";

// export class ReleaseStockInventoryUsecase {
//   constructor(private InventoryRepo: IInventoryRepository) {}

//   async execute(productId: number, quantity: number) {
//     await this.InventoryRepo.releaseStock(productId, quantity);
//   }
// }
